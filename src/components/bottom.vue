<template>
  <footer>
    
  </footer>
</template>

<script>
export default {
  /* name: 'Bottom',
  props: {
    msg: String
  },
  methods: {
    myFunction(type) {
      /* Get the text field 
      var copyText;
      const popups = document.querySelectorAll('.popup');

      if(type == 'email'){
        copyText = document.getElementById('email');
        popups[0].style.opacity="1"
        setTimeout(() => {
          popups[0].style.opacity="0"
        }, 1500);
      }
      if(type == 'phone'){
        copyText = document.getElementById('phone');
        popups[1].style.opacity="1"
        setTimeout(() => {
          popups[1].style.opacity="0"
        }, 1500);
      }

      /* Select the text field 
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field 
      document.execCommand("copy");
    } 
  } */
}
</script>
